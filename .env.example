# 唯一需要的外部金鑰；embedding 與文件解析全部在本地執行
OPENAI_API_KEY=sk-...

# 可切換的策略開關（evaluation 靠切換這兩個產生對照組）
CHUNK_STRATEGY=section        # section | fixed
RETRIEVAL_MODE=hybrid         # hybrid | vector

# embedding 後端
EMBEDDING_BACKEND=onnx        # onnx | torch | openai

STORAGE_DIR=./storage
